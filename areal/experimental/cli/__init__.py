# SPDX-License-Identifier: Apache-2.0

"""AReaL command-line interface.

Entry point: ``areal`` (registered as console script in pyproject).

Subcommands:
    areal run            Foreground orchestration driver.
    areal train start    Background training service.
    areal train stop     Stop a training service.
    areal inf start      Background inference service.
    areal inf stop       Stop an inference service.
    areal agent start    Background agent service.
    areal agent stop     Stop an agent service.
"""

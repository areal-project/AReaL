# SPDX-License-Identifier: Apache-2.0

"""Built-in :class:`AgentRunnable` implementations for the Agent Service.

Each module hosts one agent runtime that can be loaded by the Worker via
``agent_cls_path`` (e.g. ``areal.experimental.agent_service.runtimes.openclaw.OpenClawAgent``).
"""

# Compatibility constraints for the shared AIStudio Python 3.12 / SGLang image.
# Both runtime installs MUST use this file. Without it, fastembed/mem0 can pull
# versions that overwrite the image's working transformers/SGLang stack.
numpy==1.26.4
protobuf>=5.29.6,<7.0.0
huggingface-hub>=0.34.0,<1.0
tokenizers>=0.22.0,<=0.23.0
openai==2.6.1
fsspec<=2025.10.0
pillow>=10.3.0,<12.0
tensorboard>=2.17,<2.18

experiment_name: bailing_v3_grpo_nccl
trial_name: trial0

seed: 42
# Actor and rollout have disjoint GPUs; no shared TMS preload is needed.
enable_offload: false
total_train_epochs: 10
total_train_steps: 2
tokenizer_path: ${actor.path}

# Clean up external SWE sandbox instances after the run (LOG_DIR is injected).
post_exit_hook: 'AWE_ROOT=\${AWEAGENT_ROOT:-\${SWE_AGENT_ROOT:-/path/to/AReaL-SWEAgent}}; cd "$AWE_ROOT" && PYTHONPATH=$PWD:$PYTHONPATH pip install -q -r requirements.txt && AENV_SYSTEM_URL=\${AENV_SYSTEM_URL} python -m aweagent.maintenance.clean_instances'

cluster:
  # Separate 8-node actor and 8-node rollout allocations (8 GPUs per node).
  n_nodes: 16
  n_gpus_per_node: 8
  fileroot: ${oc.env:AREAL_SHARED_ROOT}/experiments
  name_resolve:
    type: nfs
    nfs_record_root: ${oc.env:AREAL_SHARED_ROOT}/name_resolve

scheduler:
  type: slurm

gconfig:
  n_samples: 2
  min_new_tokens: 0
  max_new_tokens: 16383
  max_tokens: 16383
  greedy: false
  temperature: 1.0
  top_p: 1.0
  top_k: 1000000

rollout:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  _version: v1
  backend: "sglang:d8t8p1"
  max_concurrent_rollouts: 32
  queue_size: null
  consumer_batch_size: ${train_dataset.batch_size}
  max_head_offpolicyness: 4
  enable_rollout_tracing: true
  scheduling_spec:
    - task_type: worker
      port_count: 2
      gpu: 1
      cpu: 4
      mem: 32
      image: ${oc.env:V3_ROLLOUT_IMAGE}
      cmd: python3 -m areal.infra.rpc.rpc_server
      env_vars:
        AREAL_SGLANG_FORK: theta
        PYTHONPATH: ${oc.env:THETA_SGLANG_ROOT}:${oc.env:SWE_AGENT_ROOT}:${oc.env:AREAL_ROOT}
        AENV_SYSTEM_URL: ${oc.env:AENV_SYSTEM_URL}
        SWE_AGENT_ROOT: ${oc.env:SWE_AGENT_ROOT}
        AWEAGENT_ROOT: ${oc.env:SWE_AGENT_ROOT}
  fileroot: ${cluster.fileroot}
  tokenizer_path: ${tokenizer_path}
  dump_to_file: true
  setup_timeout: 900.0
  request_timeout: 20000.0
  request_retries: 3
  pause_grace_period: 30
  agent:
    mode: inline
    # admin_api_key is injected by the launcher; not stored in YAML.
    admin_api_key: ${oc.env:SWE_RL_ADMIN_API_KEY}
    tool_call_parser: ling3
    reasoning_parser: ling3
    chat_template_type: concat
    engine_max_tokens: ${gconfig.max_tokens}
    export_style: concat
    turn_discount: 1.0
    session_timeout_seconds: 3600
    prefix_matcher: examples.swe.prefix_matchers.swe_prefix_matcher
    message_preprocessors:
      - examples.swe.preprocessors.StripAnthropicBillingHeader
      - examples.swe.preprocessors.StripAllSystemReminders
      - examples.swe.preprocessors.StripAnthropicCacheFields
      - examples.swe.preprocessors.NormalizeToolCallArguments

actor:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  # BF16 V3; the v1 controller selects NCCL through weight_update_mode=xccl.
  _version: v1
  backend: "megatron:(attn:d4p2t4c2|ffn:d4p2e8)"
  path: ${oc.env:V3_MODEL}
  weight_update_mode: xccl
  use_lora: false
  init_from_scratch: false
  disable_dropout: true
  gradient_checkpointing: true
  dtype: bfloat16
  grad_reduce_dtype: float32
  mb_spec:
    n_mbs: 1
    granularity: 1
    max_tokens_per_mb: 16384
    n_mbs_divisor: 1
  optimizer:
    type: adam
    lr: 3.0e-6
    weight_decay: 0.01
    beta1: 0.9
    beta2: 0.999
    eps: 1.0e-8
    lr_scheduler_type: constant
    gradient_clipping: 1.0
    warmup_steps_proportion: 0.001
  megatron:
    wrap_with_ddp: true
    ddp:
      grad_reduce_in_fp32: true
      overlap_grad_reduce: true
      overlap_param_gather: false
      use_distributed_optimizer: true
    use_deterministic_algorithms: false
    enable_fp32_lm_head: false
    cross_entropy_loss_fusion: false
    recompute_granularity: full
    recompute_method: uniform
    recompute_num_layers: 1
    moe_router_dtype: fp32
    moe_token_dispatcher_type: alltoall
    moe_router_bias_update_rate: 0.0
  eps_clip: 0.2
  eps_clip_higher: 0.28
  temperature: ${gconfig.temperature}
  kl_ctl: 0.0
  ppo_n_minibatches: 1
  recompute_logprob: false
  prox_logp_method: recompute
  use_decoupled_loss: true
  rejection_sampling:
    level: token
    action: mask
    metric: ratio
    upper: 5.0
  discount: 1.0
  gae_lambda: 1.0
  reward_scaling: 1.0
  reward_bias: 0.0
  reward_clip: 5.0
  reward_norm:
    mean_level: group
    std_level: group
    group_size: ${gconfig.n_samples}
    eps: 1.0e-5
  adv_norm:
    mean_level: null
    std_level: null
  max_new_tokens: ${gconfig.max_new_tokens}
  scheduling_spec:
    - task_type: worker
      port_count: 2
      gpu: 1
      cpu: 4
      mem: 32
      exclusive: true
      reservation: null
      nodelist: null
      exclude: null
      image: ${oc.env:V3_ACTOR_IMAGE}
      cmd: python3 -m areal.infra.rpc.rpc_server
      env_vars:
        NCCL_TIMEOUT: 600
        PYTORCH_CUDA_ALLOC_CONF: "expandable_segments:True"
        # Point HOME and caches at shared storage to avoid filling the
        # container's small default HOME during JIT compilation.
        HF_HOME: ${oc.env:AREAL_SHARED_ROOT}/cache/hf
        XDG_CACHE_HOME: ${oc.env:AREAL_SHARED_ROOT}/cache/xdg
        # Workers must import both AReaL and the external AReaL-SWEAgent.
        PYTHONPATH: "${oc.env:SWE_AGENT_ROOT}:${oc.env:AREAL_ROOT}"
        AENV_SYSTEM_URL: ${oc.env:AENV_SYSTEM_URL}
        SWE_AGENT_ROOT: ${oc.env:SWE_AGENT_ROOT}
        AWEAGENT_ROOT: ${oc.env:SWE_AGENT_ROOT}
        OPENAI_MODEL: "Bailing-V3"
      additional_bash_cmds:
        # Install AReaL-SWEAgent's aenv/fastmcp into the venv the workers use.
        - "/opt/.venv/bin/python -m ensurepip --upgrade >/dev/null 2>&1 || true"
        - "/opt/.venv/bin/python -m pip install --no-deps aenvironment==0.1.8rc2 || echo 'WARN: aenvironment install failed'"
        - "/opt/.venv/bin/python -m pip install 'fastmcp<3' || echo 'WARN: fastmcp install failed'"
        - "/opt/.venv/bin/python -c 'import aenv, aweagent' || echo 'WARN: aenv/aweagent import failed'"

ref:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  backend: ${actor.backend}
  path: ${actor.path}
  init_from_scratch: false
  disable_dropout: false
  dtype: ${actor.dtype}
  mb_spec:
    n_mbs: 1
    granularity: 1
    max_tokens_per_mb: 16384
    n_mbs_divisor: 1
  optimizer: null
  scheduling_strategy:
    type: colocation
    target: actor
  scheduling_spec: ${actor.scheduling_spec}

# SWE-bench agent environment. Set agent_type=cc to train Claude Code through
# the same proxy path. agent_root points at the external AReaL-SWEAgent checkout.
econfig:
  dataset_path: ""
  agent_type: swe
  agent_config: ""
  swe_agent_config: 1_0_0/min-swe-agent-train-top1
  cc_agent_config: train_cc_time3600
  agent_root: ${oc.env:SWE_AGENT_ROOT}
  swe_agent_root: ${oc.env:SWE_AGENT_ROOT}
  llm_model: ""
  opencode_provider: ""
  codex_provider: ""
  max_completion_tokens: 16383
  timeout: 3600.0

sglang:
  model_path: ${actor.path}
  random_seed: ${seed}
  skip_tokenizer_init: true
  dtype: ${actor.dtype}
  context_length: 16384
  max_prefill_tokens: 16383
  max_running_requests: 16
  mem_fraction_static: 0.85
  chunked_prefill_size: 8192
  cuda_graph_max_bs: 16
  schedule_policy: fcfs
  attention_backend: fa3
  log_level: info
  log_requests: false
  sampling_backend: pytorch
  enable_memory_saver: false
  disable_radix_cache: false
  triton_attention_num_kv_splits: 16
  disable_custom_all_reduce: true
  disable_cuda_graph: true

# Datasets: SWE-bench style jsonl with instance_id / problem_statement /
# eval_script per line.
train_dataset:
  batch_size: 8
  shuffle: true
  pin_memory: true
  num_workers: 4
  path: ${oc.env:SWE_RL_DATASET}
  type: rl
  max_length: 32768
  drop_last: true

valid_dataset:
  batch_size: 8
  shuffle: true
  pin_memory: true
  num_workers: 4
  path: ${oc.env:SWE_RL_DATASET}
  type: rl

# Utilities
saver:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: 1
  freq_steps: 10
  freq_secs: null

recover:
  mode: auto
  retries: 3
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: 1
  freq_secs: 7200
  no_save_optim: false
  no_load_optim: false
  freq_steps: 10

evaluator:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: null
  freq_steps: null
  freq_secs: null

stats_logger:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  wandb:
    mode: disabled

perf_tracer:
  enabled: false
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  session_tracer:
    enabled: false
